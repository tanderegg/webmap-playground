export function getContextData(gl) {
  const luma = gl.luma;
  if (!luma) {
    const contextState = {
      _polyfilled: false,
      _extensions: {}
    };
    gl.luma = contextState;
  }
  return gl.luma;
}
//# sourceMappingURL=context-data.js.map